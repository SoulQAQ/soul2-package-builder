# 技术栈说明（Windows 桌面程序模板）

本文档用于把当前项目的工程技术路线沉淀为“可复用模板”。目标是让下一个 Windows 程序在以下两方面与本项目保持高度一致：

- 技术栈一致：运行时、依赖、架构分层、启动与打包链路一致
- 界面风格一致：视觉语言、布局策略、交互密度、状态管理方式一致

---

## 1. 模板定位

当前模板是一个 **Python + pywebview + 原生 HTML/CSS/JS** 的 Windows 桌面应用方案，特点：

- 不引入前端框架（Vue/React）运行时，前端单文件可控
- Python 负责系统能力、文件 IO、业务规则、持久化
- pywebview 负责桌面窗口容器与 JS/Python 桥接
- UI 强调桌面工具感：冷静、克制、信息密集但可读

适用场景：

- 本地优先的数据工具
- 文件/目录驱动型业务
- 需要快速做成 EXE 分发的内部工具
- 需要保留前端灵活性但不希望引入重型构建体系

---

## 2. 技术栈总览

### 2.1 语言与运行时

- 后端语言：Python 3.13.x
- 前端语言：Vanilla JavaScript（ES6+）
- 标记与样式：HTML5 + CSS3
- 桌面容器：pywebview（Windows）

### 2.2 Python 依赖（当前项目）

`requirements.txt`：

- `pyyaml`
- `pywebview>=5.0,<6.0`

说明：

- `pywebview` 强制锁定到 5.x 区间，避免 6.x 在此类项目中的兼容问题。
- 如需扩展依赖，保持“少依赖优先”原则，先评估是否可用标准库实现。

### 2.3 关键版本约束（必须继承）

1. Python 必须是 3.13：
   - `setup.bat` 与 `start-uv.bat` 都基于 3.13
   - 3.14 在此链路上有 `pythonnet` ABI 风险，不作为模板默认版本
2. pywebview 必须默认 5.x：
   - 通过 `requirements.txt` 固定区间
   - 新项目若升级到 6.x，必须先完整回归验证 API 反射与窗口启动

---

## 3. 工程结构规范（模板目录）

建议延续当前结构：

```text
project-root/
├─ script/
│  ├─ gui.py              # pywebview 入口 + API 桥接
│  └─ core.py             # 核心业务逻辑（纯业务函数）
├─ webui/
│  └─ index.html          # 前端主界面（单文件）
├─ config/
│  └─ setting.yaml        # 程序配置（用户可编辑）
├─ data/                  # 业务数据持久化目录（运行时生成/使用）
├─ docs/
│  ├─ PROJECT_ARCHITECTURE.md
│  ├─ TECHNICAL_DESIGN.md
│  └─ 技术栈.md
├─ requirements.txt
├─ setup.bat
├─ start.bat
├─ start-uv.bat
├─ build.bat
└─ *.spec                 # PyInstaller 规格文件（可选）
```

分层职责：

- `script/gui.py`：只做桥接、路径处理、输入校验、调用业务层
- `script/core.py`：只做业务逻辑，避免 UI/窗口耦合
- `webui/index.html`：视图、交互、前端状态、API 调用编排
- `config/`：配置项（相对稳定）
- `data/`：业务数据（可变、可导入导出）

---

## 4. 后端架构标准（Python + pywebview）

### 4.1 窗口与资源路径策略

采用“开发环境 + 打包环境”双态路径：

- 开发态：以仓库根目录为资源根
- 打包态：使用 `sys.frozen` / `_MEIPASS` 定位资源目录

推荐原则：

- 所有路径统一由 `Path` 管理
- 仅在最终对外返回时转字符串
- 尽量使用相对业务路径（如 `data/spells/...`），避免硬编码绝对路径

### 4.2 API 暴露方式

统一通过 `AppApi` 暴露可序列化方法给前端，返回格式统一：

```json
{
  "success": true,
  "data": {},
  "message": "optional"
}
```

建议约定：

- 成功时 `success=true`，`message` 可省略
- 失败时 `success=false`，`message` 必填，便于前端 toast 提示
- 不抛给前端未处理异常，后端内部捕获并标准化返回

### 4.3 API 命名与兼容策略

当前项目采用 `wc3_*` 前缀作为业务域命名。模板建议：

- 新项目统一采用 `<domain>_*` 前缀（例如 `img_*`, `pack_*`）
- 前端增加方法别名映射，支持旧名回退（兼容历史版本）

这能减少“API 方法不存在”类启动故障。

### 4.4 pywebview 反射兼容规则（重要）

在 `AppApi` 中，窗口实例应保存为私有字段（例如 `_window`），不暴露为普通公共属性名（如 `window`）。  
原因：可规避部分 pywebview/pythonnet 反射场景的对象转换问题。

结论：**新项目默认沿用 `_window` 命名。**

---

## 5. 前端架构标准（单文件 index.html）

### 5.1 组织方式

单文件分区建议固定：

1. `style`：CSS 变量、布局、组件样式
2. `body`：静态结构骨架
3. `script`：状态、工具函数、事件处理、API 调用、初始化

### 5.2 状态管理模式

采用轻量全局 `state` 对象（无需框架）：

- 业务状态（当前对象、当前分类、当前模式）
- UI 状态（选中项、展开项、对话框状态）
- 配置状态（主题色、偏好设置）

更新原则：

- 所有写状态操作集中在函数中，不在多个事件回调里散写
- 关键流程在写状态后立即刷新对应视图区域（局部重绘）

### 5.3 API 调用模式

统一入口：

- 检查 `window.pywebview?.api` 可用性
- 支持方法别名解析（兼容）
- 支持启动期等待（method ready wait）
- 统一错误提示路径

这样可以显著降低“容器已就绪但方法尚不可调用”的竞态问题。

---

## 6. UI 设计系统（必须保持风格一致）

本模板的设计基调：

- calm（冷静）
- restrained（克制）
- tool-like（工具感）
- information dense but readable（高信息密度但可读）

### 6.1 样式 token 策略

所有颜色、圆角、阴影、间距基准应放在 `:root` CSS 变量中，例如：

- `--primary-color`
- `--bg-color`
- `--card-bg`
- `--border-color`
- `--shadow`
- `--radius`
- `--transition`

规则：

- 新页面不得绕过 token 直接大量写死颜色
- 业务组件应优先消费 token
- 风格迭代先改 token，再改局部组件

### 6.2 布局策略（桌面优先）

固定主结构：

- 左：资源/导航（树、列表）
- 右：编辑与详情
- 底：动作栏（关键按钮）

推荐模式：

- `display: flex` 主体横向分区
- 左右区明确宽度策略（左固定、右自适应）
- 右侧内容滚动，底部动作栏固定可见

禁止项：

- 手机化大留白
- 大面积渐变背景
- 夸张动画
- 层层套卡导致信息稀释

### 6.3 组件密度规范

控件高度与间距保持紧凑，建议：

- 文本输入高度：32~36px
- 行间距：8~12px
- 区块间距：12~16px
- 卡片/区块内边距：10~14px（避免“巨型 padding”）

### 6.4 交互规范

- 键盘优先：Tab 顺序合理、Enter/ESC 行为清晰
- hover/active 状态克制清晰
- 所有破坏性操作要二次确认
- 右键菜单用于树节点/记录项的高频管理动作

---

## 7. 数据持久化与兼容策略

### 7.1 存储原则

- 配置：`config/setting.yaml`
- 业务数据：`data/` 目录下 JSON 文件
- 导入导出：统一落在 `data/exports`（或业务定义导出目录）

### 7.2 数据目录标准化

延续当前模式：

- `data/spells/`（业务对象）
- `data/colors/`（视觉配置）
- `data/exports/`（导出文件）
- `data/settings.json`（前端偏好/运行时设置）

新项目可替换领域词，但保留同样目录层级逻辑。

### 7.3 兼容性策略（强制）

对历史数据必须“可读”：

1. 读取时做 `migrateData`
2. 缺失字段补默认值
3. 老字段映射到新字段
4. 新结构保存时尽量统一格式，但不破坏旧数据导入

示例策略：

- 参数格式从旧格式迁移到新格式时，解析层同时支持两种输入
- 界面只展示新格式，持久化可统一为新格式

---

## 8. 启动与环境管理标准

### 8.1 三套脚本职责

1. `setup.bat`
   - 校验 Python 3.13
   - 创建/重建 `.venv`
   - 安装依赖
2. `start.bat`
   - 走 pip 体系的本地启动入口
3. `start-uv.bat`
   - 走 uv 体系的本地启动入口
   - 自动创建 venv + 安装依赖 + 启动 GUI

### 8.2 版本防线

- 在脚本中显式声明 Python 3.13
- 错误提示写明 3.14 不作为默认支持版本
- 依赖安装失败时直接中止并输出可行动提示

### 8.3 Windows 启动验证标准

批处理脚本验证必须在 `cmd.exe` 语境下进行，不以 bash 结果替代。  
原因：`.bat` 是 CMD 语法，跨 shell 结果可能失真。

---

## 9. 打包发布标准（PyInstaller）

### 9.1 构建方式

`build.bat` 负责：

- 确保环境就绪
- 清理旧产物
- 以 `--onefile --windowed` 构建
- 注入静态资源和图标

### 9.2 资源打包规则

必须明确打入：

- `webui`
- `config`
- `app.ico`

示例参数（当前模板）：

- `--add-data "webui;webui"`
- `--add-data "config;config"`
- `--icon "app.ico"`

### 9.3 运行时读取规则

打包后依赖 `sys.frozen` / `_MEIPASS` 定位资源。  
新项目必须保持该分支逻辑，否则 EXE 环境将找不到前端页面或配置。

---

## 10. 下一个项目的“对齐清单”

用于你在新目录开项目时快速自检。

### 10.1 必须复制的骨架

- `script/gui.py`（桥接模板）
- `script/core.py`（业务模板）
- `webui/index.html`（单文件 UI 模板）
- `setup.bat` / `start.bat` / `start-uv.bat` / `build.bat`
- `requirements.txt`（先保留 pyyaml + pywebview 5.x）

### 10.2 必须先改的项目标识

- 程序标题（窗口标题、脚本 title、README 名称）
- 业务前缀 API（`wc3_*` 替换为新领域前缀）
- `data/` 下领域目录名
- README 与 docs 中的业务描述

### 10.3 必须保持不改的技术底座

- Python 3.13
- pywebview 5.x 区间
- 单文件前端 + pywebview API 桥接模式
- `_window` 私有字段模式
- 方法别名兼容调用策略

### 10.4 首次启动验收

1. `setup.bat` 成功
2. `start.bat` 成功
3. `start-uv.bat` 成功
4. `.venv/Scripts/python.exe script/gui.py` 可直接启动
5. 前端初始化不报 “API 方法不存在”
6. 关键 CRUD、保存、导入导出可跑通

---

## 11. 风格复用建议（让新程序“看起来就是同一家”）

### 11.1 视觉继承

- 沿用同一套 token 命名
- 沿用左树右编辑底部动作栏版式
- 沿用轻量描边、低对比阴影、克制高亮

### 11.2 交互继承

- 统一 toast 反馈语气与位置
- 统一确认弹窗结构（标题、正文、确认/取消）
- 统一右键菜单布局（重命名/复制/删除/导出等）

### 11.3 文案继承

- 按钮文案短促、动作导向
- 错误文案可执行（告诉用户下一步）
- placeholder 给最小可用示例，不写教学长文

---

## 12. 结论

这个模板的核心价值不是“某个单点技术”，而是以下组合：

1. Python 业务能力 + pywebview 桌面容器
2. 单文件前端高可控 + 轻依赖
3. 稳定的 API 桥接与兼容策略
4. 可复制的桌面工具风格系统
5. 可落地的 Windows 启动/打包链路

如果下一个程序完整继承本文件的“底座约束 + 风格约束 + 兼容约束”，就能做到与当前程序在技术栈和界面体验上高度统一。

